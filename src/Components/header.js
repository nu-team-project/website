//CSS
import '../CSS/header.css';

/**
 * Header
 * 
 * Header component for the admin page
 * 
 * @author Matthew Shaw
 * @returns JSX
 */
export default function Header()
{
    return(
        <header>
            <h1>Admin page</h1>
        </header>
    );
}